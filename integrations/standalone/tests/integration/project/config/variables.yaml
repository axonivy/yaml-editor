# yaml-language-server: $schema=https://json-schema.axonivy.com/app/11.4.1/variables.json
# == Variables ==
#
# You can define here your project Variables.
#
Variables:
  microsoft:
    connector:
      # Your Azure Application (client) ID
      appId: MyAppId
      # Secret key from your applications "certificates & secrets"
      #[password]
      secretKey: ${decrypt:S2z4BHUX2LDnX7E7KsDxIg==}
